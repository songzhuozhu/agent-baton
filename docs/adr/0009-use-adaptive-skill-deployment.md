# 使用自适应方式部署 Skill

AgentBaton 不把符号链接或文件复制设为跨平台唯一方案。每个 Agent Adapter 按目标 Agent 的原生能力、操作系统和文件系统选择部署方式：优先使用原生启停机制，其次使用符号链接或 Windows Junction，无法可靠链接时使用受控复制。每个 Skill Installation 记录部署方式和内容哈希；副本被外部修改时标记为安装漂移并交由用户处理，不静默覆盖。这样既避免符号链接在 Windows、跨卷和权限环境中的脆弱性，也避免复制方案产生不可追踪的多个事实源。
