# 按 Agent 独立验证兼容性

AgentBaton 的托管库保存不经语义改写的规范 Skill 内容，每个 Agent Adapter 分别报告兼容、兼容但有警告、不兼容或未知。Adapter 可以生成部署所需的外围配置，但不得静默改写指令正文；明确不兼容时阻止应用，状态未知时允许用户确认后尝试并验证。V1 不支持同一 Skill 的多 Agent 内容变体，以保持规范副本、上游差异和同步结果可解释。
