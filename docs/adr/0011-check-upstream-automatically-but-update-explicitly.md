# 自动检查上游但显式确认更新

AgentBaton 可以在应用启动时、距上次检查超过 24 小时后以及用户手动请求时读取上游来源 Skill 的远端版本状态，但检查更新不修改本地内容。用户发起更新后，新内容先进入隔离暂存区并展示版本、文件差异和受影响 Agent；只有用户确认后才替换托管库的规范副本，随后生成应用计划而不自动部署。已分叉 Skill 阻止直接更新，上游仓库或路径失效时保留当前版本并标记上游不可用。这样把更新可见性与第三方内容修改权限分离。
