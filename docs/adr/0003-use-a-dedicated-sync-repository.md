# 使用专用的单一同步仓库

V1 同一时间只连接一个由 AgentBaton 专用的 GitHub 仓库，用于保存参与同步的 Skill 和管理元数据，不接受业务代码仓库作为同步目标。该边界避免业务提交、用户文件和 AgentBaton 管理操作混杂，也将本机 `Local Only` 数据、AgentBaton 跨设备状态和第三方 Skill 上游明确分离；内部身份设计应允许未来扩展多个同步仓库，但 V1 不暴露该复杂度。
