# 托管库是托管 Skill 的主数据来源

Skill 被用户显式纳管后，AgentBaton 在托管库中维护唯一规范副本，各 Agent 目录中的 Skill Installation 只是由它生成的设备投影；上游更新、Git 同步和回滚都先作用于规范副本，再通过应用计划部署。外部修改安装目录会被识别为安装漂移，用户可以采纳修改或重新部署，但安装副本不会自动反向覆盖托管库，以避免多个 Agent 副本争夺主数据所有权。
