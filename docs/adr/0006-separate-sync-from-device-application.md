# 分离仓库同步与设备应用

AgentBaton 将可移植的期望 Agent 状态同步到专用仓库，但将实际安装路径、Skill Installation、扫描目录、授权凭据和 `Local Only` 数据保留在设备本地。新设备拉取后只恢复期望状态，随后根据本机观测状态生成应用计划，必须经用户确认才修改 Agent；该分离避免跨平台路径失效、首次拉取覆盖本机内容，以及把“仓库已同步”误认为“设备已配置”。
