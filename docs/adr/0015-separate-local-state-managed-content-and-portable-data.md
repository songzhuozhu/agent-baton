# 分离本地状态、托管内容与可移植数据

AgentBaton 使用带迁移版本的 SQLite 保存单台设备的配置、扫描结果、观测状态、缓存和操作记录；托管库以普通目录保存规范 Skill 内容；Git 同步仓库只保存允许跨设备同步的 Skill 文件和声明式元数据，不同步 SQLite。可移植声明按 Skill、分组和 Agent 期望状态拆分，使用稳定 UUID、确定性 JSON 和显式 `schemaVersion`。这种三层结构让本地查询、文件部署、Git 审查、冲突合并和未来格式迁移各自保持清晰边界。
