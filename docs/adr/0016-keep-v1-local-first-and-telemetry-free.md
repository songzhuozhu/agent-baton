# V1 本地优先且不默认采集遥测

AgentBaton V1 不要求产品账号，也不把 Skill 内容、描述、路径、Agent 列表、分组或日志发送到 AgentBaton 服务。联网仅限用户授权的 GitHub 同步、已记录上游的更新检查及产品自身必要的版本检查；V1 不包含产品分析、行为埋点或远程日志。错误日志留在本机并脱敏，诊断包只能由用户手动生成，生成前展示内容且默认排除 Skill 正文、凭据和绝对路径。未来遥测必须明确选择加入。
