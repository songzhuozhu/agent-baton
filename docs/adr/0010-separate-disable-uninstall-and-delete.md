# 区分移出分组、禁用、卸载与删除

AgentBaton 将四种影响范围不同的操作分开表达：移出分组只修改成员关系；对 Agent 禁用只修改该 Agent 的期望状态；从 Agent 卸载只移除本机 Installation，并在必要时同步调整期望状态；删除托管 Skill 才会从所有分组、Agent 期望状态和同步快照中全局移除。全局删除必须展示影响范围、二次确认并进入默认保留 30 天的回收站，同时通过删除墓碑传播到其他设备。AgentBaton 永远不直接删除发现但未托管的外部原始 Skill，以限制误操作和跨设备扩散的破坏范围。
