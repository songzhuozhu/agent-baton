# 更新记录

## 0.1.1-preview.1 — 2026-09-19

本版为源码预览版，尚未完成 V1 验收，不包含签名安装包。

### 使用体验

- 用应用内表单创建和重命名分组，支持 Enter 提交与失败后保留输入。
- 增加全部、已托管、已发现筛选；搜索同时覆盖托管项和发现项。
- 空库提供纳管与扫描入口，无匹配结果时可一键清除筛选。
- 原生弹窗支持 Escape、焦点返回，并在弹窗内显示错误。
- 异步操作期间阻止重复提交，显示处理状态；补齐遗漏的错误处理。
- 改善浅色主题、键盘焦点和窄桌面窗口布局，支持减少动态效果。
- Tag 输入接受中英文逗号。

### 可靠性

- 按实际发现的托管 Skill 集合判断待应用，修复同数量替换时漏报和外部项造成的数量误差。
- 单个扫描根读取失败时继续扫描其它位置。
- 纳管确认重新验证预览内容，拒绝预览后变化的 Skill。
- 删除恢复遇到数据库错误时保留原回收站路径，允许重试。
- 安全清理仍指向原托管位置的悬空安装链接，保护被外部改写的链接。
- Git 同步仅提交 `agent-baton/`，不夹带用户预先暂存的其它文件。
- 同步读写校验实体 ID、引用、来源与文件名，阻止路径越界。
- 同步恢复前生成基线，以事务提交元数据；失败回滚，并在确认时重新检查本地冲突与删除墓碑。

### 开发与验证

- 修正 Windows 测试对路径分隔符和 POSIX 可执行权限位的错误假设。
- 提供 `.nvmrc`、中文问题反馈/体验建议表单和贡献说明。
- 三平台源码 CI 全部通过后，自动为新的预览版本创建仅源码 GitHub Release。
- 本机 `npm run check`：37 个测试文件、121 个测试通过，类型检查与生产构建通过。
- 浏览器隔离示例数据验证了深浅主题、弹窗错误反馈、Escape 焦点返回及 980 × 680 布局。
- 这些证据不代表真实 Agent 加载、签名安装包或完整跨平台桌面验收。

### 升级提示

本版未改变同步格式版本。手工编辑的同步 JSON 必须使用与实体 ID 一致的文件名，ID 只允许英文字母、数字和连字符；不符合规则的文件会被拒绝，应先备份再修正。

### 已知限制

- 多文件撤销中途失败后的补偿仍需完善。
- 实时发现与 Apply Plan 的存储状态尚未完全统一，同一 Skill 的内容更新可能未在首页准确提示。
- 直接指向托管库的符号链接会提前反映库内容变更。
- 上游更新候选内容复查、同步冲突决议、GitHub 授权和真实平台验收仍在推进。

详细验收条件见[开发路线](docs/roadmap.md)和[验收审计](docs/verification/v1-gap-audit.md)。
